import { adminQuizInfo } from './quiz.js';
import { clear } from './other.js';

// beforeEach(() => {
//   clear();
// })

test('Test with valid inputs', () => {
  
  
});


test('Test with invalid authUserId', () => {
  
});


test('Test with invalid quiz id', () => {
  
});

test('Test with user not having ownership of quiz', () => {
  
});