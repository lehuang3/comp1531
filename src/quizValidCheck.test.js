import { quizValidCheck } from './quizValidCheck.js';
import { clear } from './other.js';
import { getData, setData } from 'dataStore.js';

beforeEach(() => {
  clear();
})

test('Test when quiz is valid', () => {
  let data = getData();
  setData


});
